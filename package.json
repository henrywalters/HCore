{
  "name": "hcore",
  "version": "1.2.0",
  "description": "A TypeScript library of core elements to be used for different apps.",
  "main": "dist/index.js",
  "scripts": {
    "test": "node dist/tests/run.test.js",
    "build": "tsc",
    "prod": "tsc && npm run test",
    "watch": "find src/ | entr tsc",
    "demo-serve": "node demo/server.js",
    "demo-build": "cd demo && webpack && cd ..",
    "demo-watch": "cd demo && webpack --watch && cd .."
  },
  "author": "Henry Walters",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^16.0.0",
    "ts-loader": "^9.4.1",
    "typescript": "^4.1.3",
    "webpack-cli": "^4.10.0"
  },
  "types": [
    "node"
  ],
  "dependencies": {
    "jimp": "^0.16.2"
  }
}
